{
  "name": "lunar-lander",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Lunar Lander — pilot a lunar module to a soft landing. A SceneryStack port of the PhET Flash simulation.",
  "license": "MIT",
  "keywords": [
    "physics",
    "simulation",
    "scenerystack",
    "phet",
    "lunar-lander"
  ],
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "biome check .",
    "format": "biome format --write .",
    "fix": "biome check --write .",
    "check": "tsc --noEmit && tsc -p tsconfig.scripts.json --noEmit",
    "icons": "tsx scripts/generate-icons.ts",
    "sanity": "tsx scripts/sanity/physics-sanity.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "scenerystack": "^3.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^22.10.0",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.34.5",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vite-plugin-pwa": "^1.3.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
